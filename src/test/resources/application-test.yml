spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration

security:
  jwt:
    secret: p0VmzQXQGfF/HrZ+r/Mgm1XbSAnmwyBx7Lb9nxYZQL8=
    refresh-secret: ofKPumyXoHIfARG+X4kRSs4Qbnh6DMbq0pfJiLyPvi8=
    access-exp-minutes: 30
    refresh-exp-days: 14

logging:
  level:
    org.hibernate.SQL: info
    org.hibernate.orm.jdbc.bind: warn
